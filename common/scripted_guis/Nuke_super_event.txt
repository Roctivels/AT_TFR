scripted_gui = {
	nuke_popup_window = {
		window_name = "nuke_popup_window"
		context_type = player_context
		#parent_window_token = top_bar

		visible = {
			has_country_flag  = show_nuke_popup
		}

		effects = {
		}

		triggers = {
			Super_Event_Image_Nuke_visible = {
				has_country_flag  = show_nuke_popup
			}
		}
	}
}