diplomatic_decisions = {
	ALB_retake_territory = {
		name = "Reclaim Rightful Territory"
		icon = GFX_decision_generic_flag
		allowed = { original_tag = ALB }
		visible = { FROM = { NOT = { is_core_of = ALB } } }
		state_target = yes
		on_map_mode = map_only
		available = { FROM = { is_controlled_by = ROOT } }
		complete_effect = { FROM = { add_core_of = ALB } }
		ai_will_do = { base = 1 }
	}
}