SOV_controlled_europe_request_assistance_cat = {
	icon = united_states
	visible = {
		OR = {
			original_tag = HUN
			original_tag = SLO
			original_tag = MLD
			original_tag = ROM
			original_tag = BUL
		}
		is_in_faction_with = SOV
		has_global_flag = SOV_first_nato_war_victory
	}
	visible_when_empty = yes
}
